# 执行记录

本文件是后续实施者逐任务追加的证据日志。创建蓝图时**没有**执行任何 R0～R12 任务；下方只是模板，不代表测试已通过。

## 记录模板（复制后填写）

```text
任务 ID：T__
日期 / 分支 / 提交：
实施前 Git 状态与已有改动：
本次规则决策（链接规则决策 ID）：
修改文件与作用：
新增/修改公共接口及兼容方式：
Godot 版本和可执行文件：
解析命令、退出码与 Output：
启动命令、退出码与 Output：
专项测试命令、退出码与断言：
相关回归命令、退出码：
GUI / 人工检查步骤、实际结果；未执行的原因：
发现的问题与修复、复测结果：
共享 Resource / 重开 / 空引用检查：
已知问题与下一任务进入条件：
```

---

## T01 当前版本回归基准和规则定案

```text
任务 ID：T01
日期 / 分支 / 提交：2026-09-25 / main / 尚未提交（本任务只产生文档与规则记录）
实施前 Git 状态与已有改动：M docs/CHANGELOG.md；M docs/整理设计/README.md；未跟踪 docs/实施蓝图/、docs/整理设计/逐步实施方案.md、docs/后续新加玩法。以上均由用户预置，本次未覆盖、未删除。
本次规则决策（链接规则决策 ID）：D01、D02、D03、D07 依用户授权采用“建议”首版并改为“已确认”（见规则决策.md，2026-09-25）；D04、D05、D06、D10、D11、D12 保持“待确认”，其中 D04 为 T05 阻塞点，D05 为 T06 阻塞点。
修改文件与作用：docs/实施蓝图/规则决策.md（D01/D02/D03/D07 定案）；docs/实施蓝图/执行记录.md（本记录）；docs/实施蓝图/任务索引.md（T01 状态）；docs/TASKS.md、docs/CHANGELOG.md（同步）。
新增/修改公共接口及兼容方式：无。T01 不新增玩法代码，不修改任何脚本、场景或 Resource。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`<GODOT> --headless --log-file /tmp/t01_parse.log --path . --editor --quit` → 退出码 0；Output 无解析错误。
启动命令、退出码与 Output：`<GODOT> --headless --log-file /tmp/t01_boot.log --path . --quit-after 300` → 退出码 0；无阻断性错误。
专项测试命令、退出码与断言：T01 无新专项。既有 `v2_art_validation.gd` → 退出码 0。
相关回归命令、退出码：tests/smoke 下 27 项快速冒烟（排除 120s soak）全部退出码 0（advanced_upgrade、arsenal、attack_range、automatic_weapon、boss_victory、damage_feedback、difficulty_density、difficulty_director、ending_restart、enemy_spawner、enemy_tracking、experience_flow、experience_gem、health_damage、input_map、level_progression、level_up_flow、pet_presentation、pixel_art、player_movement、projectile_base、projectile_definition、projectile_weapon_integration、targeting_service、upgrade_system、weapon_controller、weapon_definition）；120s `enemy_spawner_soak_test.gd` → 退出码 0，30/60/90/120 秒均 30 个敌人。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行完整五分钟人工游玩**。本环境无法进行真实人工操作与听感/手感判断，按验证协议标记为未完成，不冒充通过。
发现的问题与修复、复测结果：未发现阻断性问题。既有文档/代码不一致仅限计划层：当前已实现 4 把武器（星光魔杖/飞叶刃/回旋骨棒/环绕铃铛）与 21 项升级、6 类敌人（基础/快速/xiaosi/xiaoqi/Boss），比 `逐步实施方案.md` 描述的“四把武器”更细；`逐步实施方案.md` 关于“无开局菜单、无六格装备、无防具、无单件等级/质变、无金币存档”的判断与当前源码一致，仍然准确。
共享 Resource / 重开 / 空引用检查：本任务未改代码。既有 `ending_restart_smoke_test.gd` 通过，连续三次重开无旧节点、等级、经验、计时、击杀残留。共享 `.tres` 未被任何测试回写。
已知问题与下一任务进入条件：完整五分钟人工游玩与 macOS 目标机稳定性仍未验收；D04（每件基础等级/增量/质变门槛/专属上限）仍待确认。T02 依赖 D01（已确认）与 T01 基准，可在不新增玩法代码的前提下进入。
```

### T01 附：现状清单（作为 T02 的输入）

武器（`data/weapons/`，均数据驱动、共用 `WeaponController`）：

| ID | 名称 | 攻击形态 | 冷却 | 弹数 | 扩散 | 武器射程 | 弹体 | 获取途径 |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- | --- |
| `starter_weapon` | 星光魔杖 | 直线 | 1.0 | 1 | 18° | 900 | basic_projectile | 角色起始武器 |
| `leaf` | 飞叶刃 | 直线 | 0.65 | 1 | 22° | 850 | leaf_projectile | `acquire_leaf` 升级 |
| `bone` | 回旋骨棒 | 回旋（RETURNING） | 2.4 | 1 | 30° | 650 | bone_projectile | `acquire_bone` 升级 |
| `bell` | 环绕铃铛 | 环绕（ORBIT） | 3.3 | 2 | 360° | 300 | bell_projectile | `acquire_bell` 升级 |

升级池（`game_session.tscn` 共 21 项）：damage_up、fire_rate_up、projectile_count_up、move_speed_up、max_health_up、heal、bonus_projectile_chance_up、projectile_lifesteal_up、pickup_range_up、repeat_shot_chance_up、pierce_up、projectile_speed_up、projectile_size_up、critical_up、regeneration、leaf_mastery、bone_mastery、bell_mastery、acquire_leaf、acquire_bone、acquire_bell。

胜负与重开：玩家 `actor_died` → `end_run(DEFEAT)`；Boss `actor_died` → `end_run(VICTORY)`；`EndPanel` 重开按钮 → `restart_run()` → `reload_current_scene()`；`GameResult` 含 outcome/elapsed_seconds/level/kill_count。

既有公共接口（T02+ 需兼容）：`PlayerActor.add_weapon(definition, allow_duplicate=false)`、`has_weapon(id)`、`configure_weapons(...)`、`weapon_added` 信号；`UpgradeDefinition.required_weapon_id`、`weapon_definition`、`UpgradeType.ACQUIRE_WEAPON`；`UpgradeSystem.can_offer/request_choices/apply_choice`；`GameSession.start_run()/advance_time()/end_run()/restart_run()` 与导出字段 `player_definition`。

---

## T02 角色/武器目录与单局配置快照

```text
任务 ID：T02
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01 已提交；docs/后续新加玩法（空文件）保持未跟踪，未触碰。
本次规则决策（链接规则决策 ID）：D01（已确认）——候选池最多 10 个唯一 ID、库存不足可少选、起始武器必须在池内。
修改文件与作用：
- data/catalog/content_catalog.gd（新增）：只读角色/武器目录 Resource，按 ID 查询与 validate()。
- data/catalog/default_catalog.tres（新增）：默认目录，含 player_default 与四把现有武器。
- scripts/core/run_loadout.gd（新增）：运行时单局配置快照 RunLoadout，复制数组、校验、解析。
- scripts/core/game_session.gd（修改）：新增导出 content_catalog/auto_start、运行时 run_loadout、set_run_loadout()、run_started 信号，并在 start_run 解析角色与起始武器，无配置时回退旧 player_definition。
- scripts/ui/session_controls.gd（修改）：玩家可能在 start_run 之后才存在，改为经 run_started 延迟连接并对空玩家安全。
- scenes/gameplay/game_session.tscn（修改）：GameSession 挂载默认目录。
- tests/smoke/run_loadout_smoke_test.gd（新增）：目录/快照/边界/隔离/两条启动路径。
新增/修改公共接口及兼容方式：
- ContentCatalog.get_character/get_weapon/get_character_ids/get_weapon_ids/validate。
- RunLoadout（RefCounted）：_init(character_id, candidate_weapon_ids, starting_weapon_ids)、MAX_CANDIDATE_WEAPONS=10、static default_for(catalog, character_id=&"")、copy()、validate(catalog)、is_valid(catalog)、resolve_character/resolve_starting_weapons/resolve_candidate_weapons。
- GameSession：@export content_catalog、@export auto_start=true、set_run_loadout(loadout)、signal run_started(player)。旧入口不传配置时行为不变。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/run_loadout_smoke_test.gd` → 0。断言：默认目录校验通过；默认快照含全部 4 把武器且起始武器在池内；未知角色/未知武器/重复候选/起始越界/空起始/超 10 个均被识别；库存不足十个合法；构造与 copy() 数组隔离；注入配置后外部清空数组不影响本局且按快照装备 leaf；旧直启仍装备 starter_weapon。
相关回归命令、退出码：`tests/smoke` 28 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（菜单在 T03）。本环境无法进行真实人工点击与视觉验收，标记未完成。
发现的问题与修复、复测结果：首次运行新测试时 `SessionControls.initialize` 在 auto_start=false 下访问空 player，产生 SCRIPT ERROR；已改为 run_started 延迟连接并对空玩家安全，复测无错误。
共享 Resource / 重开 / 空引用检查：未回写任何 `.tres`；`content_catalog` 与 `RunLoadout` 只读/复制；`ending_restart` 回归通过，重开重建玩家与武器。GameSession 无效配置会 push_error 并回退默认角色，保证可启动。
已知问题与下一任务进入条件：完整五分钟人工游玩仍未完成；候选池目前未被升级池消费（T04）；`default_catalog` 只有 1 角色 4 武器，未制造虚假条目。T03 依赖本任务的目录与快照，可以进入。
```

---

## T03 主菜单选择与兼容启动

```text
任务 ID：T03
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01、T02 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D01（已确认）——候选池最多 10、起始武器必须在池内；菜单只负责选择与发信号。
修改文件与作用：
- scripts/ui/main_menu.gd（新增）：MainMenu，按目录生成角色/武器按钮，校验并发出 start_requested(loadout)。
- scenes/ui/main_menu.tscn（新增）：主菜单版式（标题、角色、候选武器、提示、开始按钮）。
- scripts/core/game_entry.gd（新增）：GameEntry 顶层路由，主菜单 ↔ 单局；start_run 前注入 RunLoadout；静态记住上次配置。
- scenes/bootstrap/game_entry.tscn（新增）：入口场景，挂载默认目录、菜单场景、单局场景。
- project.godot（修改）：主场景切换为 game_entry.tscn；main.tscn 保留为默认直启入口。
- tests/smoke/main_menu_smoke_test.gd（新增）：选择、上限、起始武器保护、无效配置、重复点击、入口路由、旧直启。
新增/修改公共接口及兼容方式：
- MainMenu：signal start_requested(loadout)、@export catalog、initialize()、preselect()、select_character()、set_weapon_selected()、is_weapon_selected()、get_selected_weapon_ids()、get_character_id()、get_selected_loadout()、request_start()。
- GameEntry：class_name、show_menu()、get_active_session()、get_menu()、static clear_remembered_loadout()。
- 兼容：main.tscn 仍直接启动一局，全部旧测试不受影响；菜单路径通过 GameEntry 注入配置后再 start_run。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；主场景已为入口菜单，无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/main_menu_smoke_test.gd` → 0。断言：默认选择有效且选中全部现有武器；11 武器目录默认选满 10、超限拒绝、腾位可换、起始武器不可取消；无起始武器配置无法开始且不发光信号；开始后重复请求被拒绝；入口可开始、可返回菜单并重建菜单；旧直启 main.tscn 仍有起始武器。
相关回归命令、退出码：`tests/smoke` 29 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**。需要在 1280×720 窗口实际点击角色/武器、观察中文排版与焦点，本环境无法进行真实人工操作与视觉判断，标记未完成。
发现的问题与修复、复测结果：无阻断问题。实现中发现 `SessionControls` 依赖 start_run 时已存在的玩家，T02 已改为 run_started 延迟连接，使菜单先建 UI 再开局的流程安全。
共享 Resource / 重开 / 空引用检查：未回写任何 `.tres`；菜单与入口只持有 RunLoadout 副本；结算后重开经 GameSession.restart_run()→reload_current_scene() 重新加载入口（回到菜单并按记忆预选）；entry/show_menu 释放单局。
已知问题与下一任务进入条件：主菜单尚未接入存档/解锁/商店（T27～T30）；未做菜单美术（F03，T34）；人工 GUI 与五分钟游玩未完成。T04 依赖 T02 目录/快照，可以进入。
```

---

## T04 六格装备清单与升级获取过滤

```text
任务 ID：T04
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T03 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D02（已确认）武器/防具共用六格、同 ID 不重复、升级不占新格；D03（已确认）获取走升级三选一、未持有专属不出现。
修改文件与作用：
- scripts/actors/player_actor.gd（修改）：新增 MAX_EQUIPMENT_SLOTS=6、equipment_changed 信号、候选武器 ID 与装备 ID 列表，及 configure_equipment()、can_acquire()、try_acquire_weapon()、get_equipped_count()/ids、has_equipment()、is_equipment_full()；add_weapon 登记装备，起始武器同样占格；ACQUIRE_WEAPON 改走唯一获取入口。
- scripts/systems/upgrade_system.gd（修改）：ACQUIRE_WEAPON 过滤改为 player.can_acquire()，统一覆盖候选池、重复与满格；已有武器专属升级逻辑不变。
- scripts/core/game_session.gd（修改）：start_run 注入候选武器 ID（来自 RunLoadout，旧直启为空表示不限制）。
- scripts/ui/session_controls.gd（修改）：武器栏显示“装备 n/6”，并监听 equipment_changed。
- tests/smoke/equipment_slots_smoke_test.gd（新增）：容量、候选、重复、满格、升级过滤与重开。
新增/修改公共接口及兼容方式：
- PlayerActor：MAX_EQUIPMENT_SLOTS、signal equipment_changed(equipped_ids)、configure_equipment(candidate_weapon_ids)、get_candidate_weapon_ids()、get_equipped_ids()、get_equipped_count()、has_equipment(id)、is_equipment_full()、can_acquire(definition)、try_acquire_weapon(definition)。
- 兼容：`add_weapon()`、`has_weapon()` 保留；旧直启候选列表为空，不限制获取，旧测试不受影响。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/equipment_slots_smoke_test.gd` → 0。断言：起始武器占 1 格；非候选武器不可获取；1+5 恰好满 6 格、第 7 件被拒；重复获取不占格；equipment_changed 触发 5 次；候选内空位提供获取卡、非候选不提供、通用升级始终可用；满格后获取卡消失且三选一无 ACQUIRE_WEAPON；重开后装备回到 1 件且无残留。
相关回归命令、退出码：`tests/smoke` 30 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（装备栏/三选一卡面需实际窗口观察），本环境无法人工操作，标记未完成。
发现的问题与修复、复测结果：测试首次用闭包累加整数计数失败（GDScript lambda 按值捕获），改为数组容器计数后通过；无产品代码问题。
共享 Resource / 重开 / 空引用检查：未回写任何 `.tres`；候选与已装备 ID 均为运行时副本；restart 断言通过；UI 对空玩家安全。
已知问题与下一任务进入条件：装备目前只接武器，防具留 T08/T11；未处理“替换/丢弃”（D02 首版不做）；人工验收未完成。T05（单件等级/质变）依赖 T04，但受 D04“待确认”阻塞，需用户先定规则。
```

---

## T05 单件装备等级/质变运行时模型

```text
任务 ID：T05
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T04 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D04 经用户确认后由“待确认”改为“已确认”（2026-09-25）：基础等级 1→5；基础升级每级 +12% 伤害；基础满级后两条互斥质变、每件每局一次；分支专属升级 3 级；样板星光魔杖分支：星屑散射（弹数 +1）、星辉聚焦（伤害 +25%、弹速 +20%）。
修改文件与作用：
- scripts/core/equipment_progress.gd（新增）：EquipmentProgress 运行时对象，保存 base_level、branch_id、branch_upgrade_levels，提供 copy() 只读快照与升级/质变校验。
- scripts/actors/player_actor.gd（修改）：新增 equipment_progress_changed 信号与每装备进度字典；get_progress() 返回快照；add_equipment_base_level()、can_choose_equipment_branch()、choose_equipment_branch()、add_equipment_branch_upgrade() 为唯一修改入口；initialize/configure_weapons 重置进度。
- tests/smoke/equipment_progress_smoke_test.gd（新增）：等级、门槛、互斥、专属上限、快照隔离、重复获取、Resource 隔离、重开。
新增/修改公共接口及兼容方式：
- EquipmentProgress：MAX_BASE_LEVEL=5、MAX_BRANCH_UPGRADE_LEVEL=3、copy()、is_base_maxed()/can_add_base_level()/add_base_level()、has_branch()/can_choose_branch()/choose_branch()、get_branch_upgrade_level()/can_add_branch_upgrade()/add_branch_upgrade()。
- PlayerActor：signal equipment_progress_changed(equipment_id, snapshot)、get_progress(equipment_id)（快照，未持有返回 null）、has_equipment_progress()、add_equipment_base_level()、can_choose_equipment_branch()、choose_equipment_branch()、add_equipment_branch_upgrade()。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/equipment_progress_smoke_test.gd` → 0。断言：未持有无进度；初始 1 级不可选质变；4 次升级到满、第 5 次拒绝；满级后可选的星屑散射分支选中后不可改选；专属升级 3 次到上限、不同专属独立；快照改动不污染局内；重复获取不占格；武器与子弹 `.tres` 未被回写；重开后基础等级与分支归零。
相关回归命令、退出码：`tests/smoke` 31 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（本条无可见 UI，T07 才接卡面标记），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：进度只存运行时；get_progress 返回副本；重开断言通过；未持有返回 null 而非空引用。
已知问题与下一任务进入条件：进度已建立但尚未被升级卡驱动（T06/T07）；质变分支 ID 暂为约定字符串，T07 起由升级资源统一维护。T06 依赖 T05 进度接口，可以进入。
```

---

## T06 升级候选的分类、前置与上限

```text
任务 ID：T06
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T05 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D05 由“待确认”改为“已确认（首版）”：单一升级池、均匀无放回三选一；weight 预留不启用；分类只做前置/互斥/上限过滤。D04 已确认。
修改文件与作用：
- data/upgrades/upgrade_definition.gd（修改）：新增 UpgradeCategory 枚举与 category、target_equipment_id、branch_id 字段，get_target_equipment_id() 统一解析目标装备。
- scripts/systems/upgrade_system.gd（修改）：can_offer 集中按分类过滤——通用放行、获取看 can_acquire、基础看基础等级、质变看满级且未选分支、分支专属看分支匹配与上限。
- scripts/actors/player_actor.gd（修改）：apply_upgrade 对 BASE_UPGRADE/ASCENSION/BRANCH_UPGRADE 三类走进度修改并以 _finalize_upgrade 统一记层；效果类升级保持原逻辑。
- data/upgrades/acquire_leaf.tres、acquire_bone.tres、acquire_bell.tres（修改）：category=1（ACQUIRE_EQUIPMENT）。
- tests/smoke/upgrade_category_smoke_test.gd（新增）：分类前置、互斥、上限与候选数量。
新增/修改公共接口及兼容方式：
- UpgradeDefinition.UpgradeCategory、category、target_equipment_id、branch_id、get_target_equipment_id()。
- 兼容：旧升级默认 category=GENERIC；can_offer 对 GENERIC+ACQUIRE_WEAPON 仍按获取处理；`can_offer(definition)` 直接读取 PlayerActor 中的局内配置与装备状态，不新增独立上下文对象。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/upgrade_category_smoke_test.gd` → 0。断言：通用始终可出；获取卡未持有可出、持有后不可出；基础升级持有可出、满级不可出；质变满级后可出、选分支后两条分支均不可出；分支专属需匹配分支且到上限不可出；候选充足给 3 张且不重复、不足给实际数量、全不可用给空列表。
相关回归命令、退出码：`tests/smoke` 32 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（卡面分类标记在 T07/后续 UI），标记未完成。
发现的问题与修复、复测结果：测试首次未设置候选池导致“全不可用”断言失败，补 configure_equipment 后通过；产品逻辑无问题。
共享 Resource / 重开 / 空引用检查：过滤只读；apply 仅改运行时进度；未回写 `.tres`；重开由既有回归覆盖。
已知问题与下一任务进入条件：UI 尚未显示“基础/质变/专属”标记；weight 与刷新未实现。T07 依赖 T05/T06，可以进入。
```

---

## T07 一把现有武器的样板质变

```text
任务 ID：T07
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T06 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D04 已确认（星光魔杖样板：基础 1→5 每级 +12% 伤害；基础满级后二选一互斥质变；专属 3 级；星屑散射＝弹数 +1，星辉聚焦＝伤害 +25%、专属弹速 +20%/级）；D05 首版均匀无放回。
修改文件与作用：
- data/upgrades/star_wand_base.tres（新增）：基础升级，DAMAGE_MULTIPLIER +12%，max_stacks 4，目标 starter_weapon。
- data/upgrades/star_wand_scatter.tres（新增）：质变，PROJECTILE_COUNT +1，branch_id=scatter，max_stacks 1。
- data/upgrades/star_wand_focus.tres（新增）：质变，DAMAGE_MULTIPLIER +25%，branch_id=focus，max_stacks 1。
- data/upgrades/star_wand_scatter_up.tres（新增）：分支专属，PROJECTILE_COUNT +1，branch_id=scatter，max_stacks 3。
- data/upgrades/star_wand_focus_up.tres（新增）：分支专属，PROJECTILE_SPEED +20%，branch_id=focus，max_stacks 3。
- scripts/actors/player_actor.gd（修改）：apply_upgrade 对基础/质变/专属先写进度再应用 type 效果（取消早退），使质变与专属产生真实战斗效果。
- scripts/ui/level_up_panel.gd（修改）：卡面加【获取】【基础】【质变】【专属】中文前缀。
- scenes/gameplay/game_session.tscn（修改）：升级池加入上述 5 项。
- tests/smoke/weapon_ascension_smoke_test.gd（新增）：完整链、互斥、晚获取继承与重开。
新增/修改公共接口及兼容方式：无新增接口；复用 UpgradeDefinition.category/branch_id/target_equipment_id 与 PlayerActor 进度接口。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/weapon_ascension_smoke_test.gd` → 0。断言：基础 4 级前质变不可出、4 级后伤害倍率提高且基础不可再出；选择星屑散射后弹数 +1、聚焦不可出、散射专属可出而聚焦专属不可出；散射专属 3 级后不可再出且弹数 = 1+1+3；选择聚焦后伤害提高且散射不可出；晚获取飞叶刃继承通用伤害升级；重开后等级/分支/伤害倍率归零。
相关回归命令、退出码：`tests/smoke` 33 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需实际窗口查看【基础/质变/专属】卡面与弹数变化），标记未完成。
发现的问题与修复、复测结果：实现中先误删 LevelUpPanel._clear_buttons 方法体，已恢复并新增 _category_tag；专项测试首次因未加候选池配置，已在 T06 修正。
共享 Resource / 重开 / 空引用检查：升级效果只写运行时修正与进度历史；重开归零断言通过；未回写 `.tres`。
已知问题与下一任务进入条件：其余旧武器（飞叶刃/骨棒/铃铛）尚未配质变；卡面标记未做 GUI 验收；质变数值为首版。T08（防具接口）与 T09（通用属性/射程）依赖 T04/T05，可进入。
```

---

## T08 防具静态定义与运行时装配接口

```text
任务 ID：T08
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T07 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D02 已确认（武器与防具共用六格、同 ID 不重复）；D03 已确认（获取走升级三选一）。不含任何防御数值（留 T11）。
修改文件与作用：
- data/armor/armor_definition.gd（新增）：ArmorDefinition（ID、显示、图标、ArmorCategory 盔甲/头盔/手套、预留基础/分支升级引用）与 get_category_name()。
- data/upgrades/upgrade_definition.gd（修改）：新增 UpgradeType.ACQUIRE_ARMOR 与 armor_definition 字段。
- data/catalog/content_catalog.gd（修改）：目录新增 armors 数组、get_armor() 与防具校验。
- scripts/actors/player_actor.gd（修改）：装备清单记录类别；新增 armor_acquired 信号、can_acquire_armor()/try_acquire_armor()、has_armor()/get_armor_definition()、get_equipped_category()、get_equipped_weapon_ids()/get_equipped_armor_ids()；apply_upgrade 支持 ACQUIRE_ARMOR。
- scripts/systems/upgrade_system.gd（修改）：ACQUIRE_EQUIPMENT 支持防具获取卡过滤。
- scripts/ui/session_controls.gd（修改）：装备栏分列武器与防具（含类别名）。
- tests/smoke/armor_equipment_smoke_test.gd（新增）：共存、满格、无效/重复、升级获取与重开。
新增/修改公共接口及兼容方式：
- ArmorDefinition / ArmorDefinition.ArmorCategory / get_category_name()。
- PlayerActor：signal armor_acquired(definition)、can_acquire_armor()、try_acquire_armor()、has_armor()、get_armor_definition()、get_equipped_category()、get_equipped_weapon_ids()、get_equipped_armor_ids()。
- UpgradeDefinition.armor_definition；ContentCatalog.armors/get_armor()（默认空，不制造虚假条目）。
- 兼容：武器路径不变；防具不创建 WeaponController，仍共用 T04 的槽位/重复/满格接口。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/armor_equipment_smoke_test.gd` → 0。断言：六把武器占满后防具不可获取；5 武器 + 1 防具＝6 格且类别计数正确；防具不创建控制器、不能当武器发射；空/空 ID/重复防具被拒且不占格；防具获取升级卡持有前后过滤正确；重开清空防具且装备数回到 1。
相关回归命令、退出码：`tests/smoke` 34 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需窗口查看装备栏武器/防具分列），标记未完成。
发现的问题与修复、复测结果：测试初稿误用另一局的控制器数量做断言，已改为本局获取防具前后的控制器数量比较；无产品代码问题。
共享 Resource / 重开 / 空引用检查：防具只登记运行时清单；未回写 `.tres`；null/空 ID 安全返回 false；重开断言通过。
已知问题与下一任务进入条件：仅“能被持有”，无减伤/穿戴表现；默认目录暂无防具（T11 添加基础盔甲）。T10 依赖 T09，T11 依赖 T08/T10。
```

---

## T09 通用属性 Buff 与射程语义

```text
任务 ID：T09
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T08 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D06 由“待细化”在 T09 定案：全武器射程采用“最终索敌值”语义——先取 min(角色攻击范围, 武器 target_range)，再乘角色级全武器射程倍率；弹体实际可达距离仍由弹速×寿命决定，二者互不替代。
修改文件与作用：
- data/upgrades/upgrade_definition.gd（修改）：新增 UpgradeType.ALL_WEAPON_RANGE。
- scripts/actors/actor_base.gd（修改）：新增 get_weapon_range_multiplier() 默认 1.0。
- scripts/actors/player_actor.gd（修改）：新增 _all_weapon_range_multiplier、重置、get_weapon_range_multiplier() 覆盖与 ALL_WEAPON_RANGE 应用分支。
- scripts/components/weapon_controller.gd（修改）：get_effective_target_range 在 min 结果上乘全武器射程倍率。
- data/upgrades/weapon_range_up.tres（新增）：全武器射程 +10%/级，max 5，加入升级池。
- scenes/gameplay/game_session.tscn（修改）：升级池新增该资源。
- tests/smoke/weapon_range_buff_smoke_test.gd（新增）：语义、边界、晚获取继承与重开。
新增/修改公共接口及兼容方式：
- ActorBase.get_weapon_range_multiplier()（默认 1.0，旧 Actor 行为不变）。
- PlayerActor.get_weapon_range_multiplier()；UpgradeType.ALL_WEAPON_RANGE。
- 兼容：倍率默认 1.0，既有索敌语义与 `attack_range` 测试不受影响；倍率保存在玩家，晚获取武器自动读取同一值。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/weapon_range_buff_smoke_test.gd` → 0。断言：初始有效射程 900；升级后倍率 1.1、起始武器 990；角色上限分支 min(1000,5000)×1.1=1100；武器上限分支 min(1000,300)×1.1=330；弹体速度×寿命不变且不等于索敌值；共享武器射程不变；晚获取飞叶刃为 850×1.1；重开倍率归 1。
相关回归命令、退出码：`tests/smoke` 35 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（射程变化需实战观察），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：倍率为运行时值；未回写 `.tres`；重开归位断言通过。
已知问题与下一任务进入条件：射程无可视化；通用属性表仅补了全武器射程，防御等留 T10。T10（防御/免疫/反伤基座）依赖 T09，可以进入。
```

---

## T10 防御/免疫/反伤伤害结算基座

```text
任务 ID：T10
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T09 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D07 定案——免疫→闪避→防御→扣血→反伤/吸血/死亡；实际伤害 = max(原始 − 防御, 原始 × 最低比例)，默认最低 10%；闪避/免疫按概率完全免除且给提示；反伤按实际伤害比例返还，reflect/dot 不反伤。用户要求数值可改、闪避/免疫要有提示并列出待生成素材。
修改文件与作用：
- data/combat/combat_rules.gd、data/combat/default_combat_rules.tres（新增）：可调的最低伤害比例、闪避/免疫/反伤上限。
- scripts/combat/damage_result.gd（新增）：单次结算结果（原始/实际/减免/闪避/免疫/击杀/标签）。
- scripts/actors/actor_base.gd（修改）：新增伤害/闪避/免疫/反伤运行时属性与 getter/setter/add，注入 CombatRules，重写 apply_damage 走统一管线并返回 DamageResult；仅在实际扣血后触发反伤；reflect/dot 防递归；flash 不因闪避/免疫触发。
- scripts/components/damage_feedback_component.gd（修改）：bind_actor 连接闪避/免疫信号，代码绘制“闪避/免疫”浮动文字占位提示。
- scripts/core/game_session.gd（修改）：导出 combat_rules 并下发玩家。
- data/upgrades/upgrade_definition.gd（修改）：新增 DEFENSE/DODGE_CHANCE/IMMUNE_CHANCE。
- data/upgrades/defense_up/dodge_up/immune_up.tres（新增）与 scenes/gameplay/game_session.tscn（升级池 + combat_rules）。
- docs/整理设计/闪避免疫提示素材清单.md（新增）：FDB-01～FDB-07 待生成素材、尺寸、提示词方向与接入状态。
- tests/smoke/damage_resolution_smoke_test.gd（新增）：公式、下限、零伤害、闪避/免疫提示、反伤与防递归、dot 不反伤。
新增/修改公共接口及兼容方式：
- CombatRules（Resource）；DamageResult（RefCounted）。
- ActorBase：damage_dodged/damage_immune 信号；set_combat_rules()；get/set/add_defense、dodge_chance、immune_chance、damage_reflect_ratio；get_min_damage_ratio 等；set_damage_random_seed()。
- HealthComponent.apply_damage 保持不变（接收已减免事件），旧测试语义兼容；ActorBase.apply_damage 返回值由 void 变为 DamageResult（调用方可忽略）。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/damage_resolution_smoke_test.gd` → 0。断言：防御 5 时 20 伤害扣 15、减免 5；防御极高时保留 10%（2/20）；零伤害不结算；100% 闪避不扣血、信号一次、出现“闪避”标签；100% 免疫不扣血、信号一次、出现“免疫”标签；反伤按实际伤害 50% 返还；双方反伤时 reflect 标签阻止递归；dot 不反伤。
相关回归命令、退出码：`tests/smoke` 36 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（闪避/免疫文字提示与浮出动画需窗口观察；正式图标素材待生成），标记未完成。
发现的问题与修复、复测结果：测试初稿使用了尚未存在的 set_damage_reflect_ratio，已补充该 setter；无产品逻辑问题。
共享 Resource / 重开 / 空引用检查：所有数值在 Resource 与运行时属性，未回写 `.tres`；来源为空/已释放/自身时反伤安全返回；旧 health_damage/projectile_base/advanced_upgrade 回归通过。
已知问题与下一任务进入条件：闪避/免疫仅代码文字占位，正式素材待按清单生成；反伤尚无装备来源（T21）。T11（基础盔甲）依赖 T08/T10，可以进入。
```

### T10 待生成素材

素材清单见 `docs/整理设计/闪避免疫提示素材清单.md`（FDB-01 闪避提示、FDB-03 免疫提示为优先项；FDB-05/06 反伤随 T21）。

---

## T11 基础盔甲

```text
任务 ID：T11
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T10 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D02/D03 已确认；D07 防御公式已确认。盔甲数值为首版且全部在 Resource 可调（防御 3 基础、每级 +2、移速惩罚 -8%、每级减轻 2%）。
修改文件与作用：
- data/armor/armor_definition.gd（修改）：新增 defense_base/defense_per_level/move_penalty_ratio/move_penalty_reduction_per_level 与按等级计算的方法。
- data/armor/armor_basic.tres（新增）：基础盔甲（防御 3、每级 +2、移速惩罚 8%、每级减轻 2%）。
- data/upgrades/acquire_armor_basic.tres、armor_basic_up.tres（新增）：获取卡与基础强化卡。
- data/catalog/default_catalog.tres（修改）：armors 加入基础盔甲。
- scripts/actors/player_actor.gd（修改）：盔甲防御/移速惩罚运行时汇总与刷新；BASE_UPGRADE 命中盔甲时只提升等级并刷新；DEFENSE 升级走可叠加的 _upgrade_defense_bonus。
- scripts/ui/session_controls.gd（修改）：装备栏显示防御与当前移速。
- scenes/gameplay/game_session.tscn（修改）：升级池加入盔甲获取与强化卡。
- tests/smoke/armor_basic_smoke_test.gd（新增）：防御/移速/升级减轻/满级/重复/重开。
新增/修改公共接口及兼容方式：ArmorDefinition 新增数值字段与 get_defense_for_level()/get_move_penalty_for_level()；PlayerActor 新增 get_armor_move_penalty_ratio()。旧武器路径与升级不受影响。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0。
专项测试命令、退出码与断言：`--script res://tests/smoke/armor_basic_smoke_test.gd` → 0。断言：初始防御 0/移速 220；获取后防御 3、移速 -8%、20 伤害扣 17；2 级防御 5、惩罚 -6%；满级防御 11、惩罚 0；获取卡持有前后过滤；重复获取不叠加；重开防御/移速/盔甲归零。
相关回归命令、退出码：`tests/smoke` 37 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（装备栏防御/移速显示需窗口观察；C06 图标待生成），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：数值在 Resource 与运行时属性，未回写 `.tres`；重开断言通过。
已知问题与下一任务进入条件：盔甲无穿戴立绘/图标（C06 待生成）；分支（反伤/骑士/狂战/科技）未做。T21/T22/T23/T24 依赖 T11，T12 依赖 T04/T09，均可进入。
```

---

## T12 基础弓箭

```text
任务 ID：T12
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T11 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D01 候选池已确认；D04 样板数值沿用“基础每级 +12%”。弓箭首版数值（可调）：冷却 1.2s、单发、箭伤 12、箭速 850、寿命 1.6、射程 1000。
修改文件与作用：
- data/projectiles/arrow_projectile.tres（新增）：直线箭，12 伤害/850 速度/1.6 寿命/8 命中半径；弹体外观暂复用 bolt_frames（D01 箭体素材待生成）。
- data/weapons/bow.tres（新增）：长弓，冷却 1.2、单发、射程 1000；武器外观暂复用 wand_frames（C03 弓素材待生成）。
- data/upgrades/acquire_bow.tres、bow_base.tres（新增）：获取卡与基础强化卡（+12% 伤害，4 级）。
- data/catalog/default_catalog.tres、scenes/gameplay/game_session.tscn（修改）：加入长弓与两张升级卡。
- tests/smoke/bow_weapon_smoke_test.gd（新增）：获取、门槛、发射、共存与重开。
新增/修改公共接口及兼容方式：无新增接口，复用 WeaponDefinition/ProjectileDefinition/UpgradeDefinition 与 T04/T05/T06 链路。共享 `.tres` 未改。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --path . --editor --quit` → 0。
启动命令、退出码与 Output：`--headless --path . --quit-after 300` → 0。
专项测试命令、退出码与断言：`--script res://tests/smoke/bow_weapon_smoke_test.gd` → 0。断言：目录含长弓且冷却/弹体正确；未持有时基础升级不可出；获取占一格且自动索敌 1000；基础升级 4 级后伤害倍率提升且不再出现；实际 request_fire 生成 arrow_projectile；与飞叶刃共存；重开清空长弓。
相关回归命令、退出码：`tests/smoke` 38 项快速冒烟全部退出码 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（持弓姿态与箭矢朝向需窗口观察；C03/D01 素材待生成），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：数值在 Resource；重开断言通过；未回写 `.tres`。
已知问题与下一任务进入条件：无正式弓/箭素材，暂用占位；多重射击（T13）与万箭齐发（T14）依赖本任务，可以进入。
```

---

## T13 弓：多重射击

```text
任务 ID：T13
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T12 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D04/D05 已确认；“射击轮次”与“弹数+1”“穿透+1”是三个独立属性。轮间间隔配置在 WeaponDefinition.volley_interval_seconds（默认 0.18，可改）。
修改文件与作用：WeaponRuntimeModifier 新增 volley_count_bonus；UpgradeDefinition 新增 VOLLEY_COUNT；WeaponDefinition 新增 volley_interval_seconds；WeaponController 抽取 _fire_volley、新增 _fire_extra_volleys 与 get_effective_volley_count；PlayerActor 应用 VOLLEY_COUNT；新增 bow_multishot / bow_multishot_rounds / bow_multishot_pierce 三个 Resource 并加入升级池；新增 bow_multishot_smoke_test。
新增/修改公共接口及兼容方式：WeaponController.get_effective_volley_count()；WeaponRuntimeModifier.volley_count_bonus；WeaponDefinition.volley_interval_seconds；UpgradeType.VOLLEY_COUNT。默认 1 轮，旧武器行为不变。
Godot 版本和可执行文件：4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析/启动/专项/回归：解析 0；启动 300 帧 0；`bow_multishot_smoke_test.gd` 0（多重后 2 轮、发射后即冷却、0.35s 出现第二轮、轮次升 4 级但弹数仍 1、穿透独立、重置取消后续轮次）；tests/smoke 39 项快速冒烟全部 0。
GUI / 人工检查：未执行（需窗口看多轮节奏），标记未完成。
共享 Resource / 重开 / 空引用：仅运行时修正；generation 取消未完成轮次；未回写 `.tres`。
已知问题与下一任务进入条件：无正式弓/箭素材；万箭齐发（T14）依赖本任务，可以进入。
```

---

## T14 弓：万箭齐发

```text
任务 ID：T14
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T13 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D04/D05 已确认。万箭齐发只改每轮弹数，不改轮数；扩散角写在 bow.tres（25°，可改）。
修改文件与作用：data/weapons/bow.tres（扩散 25°）；新增 bow_volley（质变，弹数+1，branch=volley）与 bow_volley_count（专属，每级弹数+1，max 2）；加入升级池；新增 bow_volley_smoke_test。
新增/修改公共接口及兼容方式：无新增接口，复用 PROJECTILE_COUNT 与分支互斥机制；单发时扩散不生效，旧行为不变。
Godot 版本和可执行文件：4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析/启动/专项/回归：解析 0；启动 300 帧 0；`bow_volley_smoke_test.gd` 0（选万箭后每轮 2 支、轮数仍 1；箭雨满级每轮 4 支、轮数仍 1；一次发射生成 4 支且方向互不重叠；已选多重后万箭不可出）；tests/smoke 40 项快速冒烟全部 0。
GUI / 人工检查：未执行（需窗口看扇形箭雨），标记未完成。
共享 Resource / 重开 / 空引用：仅数据与运行时修正；未回写 `.tres`。
已知问题与下一任务进入条件：弓/箭素材仍为占位；法杖线（T15–T19）依赖 T04/T09，可以进入。
```

---

## T15 基础法杖/蓄力法球

```text
任务 ID：T15
日期 / 分支 / 提交：2026-09-25 / main / 见本次提交
实施前 Git 状态与已有改动：T01～T14 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D04/D05 已确认。法杖为新武器（不覆盖既有星光魔杖），蓄力期间可移动、可在蓄力中跟踪目标；目标消失时按最后已知位置释放；蓄力时长与倍率写在 Resource。
修改文件与作用：WeaponDefinition 新增 charge_seconds/charge_damage_multiplier；WeaponController 新增可取消蓄力状态（begin_charge/is_charging/get_charge_remaining/_release_charge），释放后从释放时起算冷却；_fire_volley 增加伤害倍率参数；新增 staff（蓄力法杖，冷却 2.0/蓄力 0.5/倍率 2.0）、staff_orb、acquire_staff、staff_base；加入目录与升级池；新增 staff_charge_smoke_test（用固定蓄力临时武器避免计时抖动）。
新增/修改公共接口及兼容方式：WeaponDefinition.charge_seconds/charge_damage_multiplier；WeaponController.begin_charge/is_charging/get_charge_remaining。charge_seconds 默认 0，旧武器不经蓄力，行为不变。
Godot 版本和可执行文件：4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析/启动/专项/回归：解析 0；启动 300 帧 0；`staff_charge_smoke_test.gd` 0（蓄力期间不发射、重复请求被拒且不重置进度、蓄力完成释放且伤害倍率 2.0、释放即冷却、重置清空蓄力、目标消失按最后位置释放）；tests/smoke 41 项快速冒烟全部 0。
GUI / 人工检查：未执行（需窗口看蓄力表现），标记未完成。
共享 Resource / 重开 / 空引用：仅运行时状态；未回写 `.tres`；空目标/离树安全。
已知问题与下一任务进入条件：法杖外观/法球为占位；爆炸（T16）等分支依赖本任务，可以进入。
```

---

## T16 爆炸与范围命中

```text
任务 ID：T16
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T15 已提交；docs/后续新加玩法（空文件）保持未跟踪，未触碰；工作树无其他改动。
本次规则决策（链接规则决策 ID）：新增 D13（已确认·首版）——爆炸弹首击引爆并停用；溅射 = 直击伤害 × 可配倍率；同次爆炸按 Actor 去重；直击目标是否再吃溅射由 hits_direct_target 配置（默认 true）；溅射不触发吸血/暴击；半径、掩码、上限、击退、特效全部在 Resource。
修改文件与作用：
- data/combat/explosion_definition.gd（新增）：ExplosionDefinition 可配爆炸档案。
- data/combat/staff_orb_explosion_profile.tres（新增）：爆炸法术默认档案（半径 90、溅射 0.5、含直击、掩码 4）。
- scripts/combat/area_hit_resolver.gd（新增）：AreaHitResolver 圆形形状查询，只查 Area、按实例去重、过滤死亡/已释放/同阵营。
- scripts/combat/explosion_effect.gd（新增）：ExplosionEffect 一次性表现节点，无素材时绘制占位圆环，时长结束自清理。
- data/projectiles/staff_orb_explosion.tres（新增）：带爆炸档案的爆炸法球（基础数值同 staff_orb）。
- data/projectiles/projectile_definition.gd（修改）：新增可选 explosion 字段。
- scripts/combat/projectile_spawn_context.gd、weapon_runtime_modifier.gd（修改）：新增爆炸范围/伤害运行时倍率。
- scripts/combat/projectile_base.gd（修改）：新增 explosion_triggered 信号；命中时执行受控范围结算并停用弹体；生成一次爆炸特效。
- scripts/components/weapon_controller.gd（修改）：新增运行时弹体覆盖与爆炸倍率，统一经 get_effective_projectile_definition() 取弹体。
- data/upgrades/upgrade_definition.gd（修改）：新增 EXPLOSION/EXPLOSION_RADIUS/EXPLOSION_DAMAGE 与 projectile_definition 字段。
- scripts/actors/player_actor.gd（修改）：apply_upgrade 支持爆炸质变切换弹体与两级专属升级；新增 _apply_projectile_override()。
- scripts/core/game_session.gd（修改）：结算 _stop_combat_nodes() 同时清理 ExplosionEffect。
- data/upgrades/staff_explosion.tres、staff_explosion_radius_up.tres、staff_explosion_damage_up.tres（新增）：爆炸质变与裂爆/灼爆专属升级。
- scenes/gameplay/game_session.tscn（修改）：升级池加入上述 3 项。
- tests/smoke/explosion_aoe_smoke_test.gd（新增）：7 组断言。
新增/修改公共接口及兼容方式：
- ExplosionDefinition（Resource，class_name）；AreaHitResolver.collect_actors()；ExplosionEffect.setup()/finish()。
- ProjectileDefinition.explosion；ProjectileSpawnContext.explosion_radius_multiplier/explosion_damage_multiplier；WeaponRuntimeModifier.explosion_radius_multiplier/explosion_damage_multiplier。
- ProjectileBase.explosion_triggered(center, hit_count)。
- WeaponController.get_effective_projectile_definition()/set_projectile_definition_override()/get_explosion_radius_multiplier()/get_explosion_damage_multiplier()。
- UpgradeDefinition.projectile_definition 与三个新枚举；PlayerActor._apply_projectile_override()。
- 兼容：explosion 为空不改变任何既有弹体与武器行为；爆炸倍率默认 1.0；旧升级与新测试不受影响。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t16_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t16_boot.log --path . --quit-after 300` → 0；无阻断性错误或重复警告。
专项测试命令、退出码与断言：`--script res://tests/smoke/explosion_aoe_smoke_test.gd` → 0。断言：中心+范围内 2 目标受击、范围外不掉血、直击目标共吃 12（直击 8+溅射 4）；重叠两个目标各只受伤一次、命中数=3；hits_direct_target=false 时直击目标只吃 8、周围吃 4；穿透 5 的爆炸弹只引爆一次、停用且剩余穿透仍为 5；已释放目标被跳过且 Boss 吃溅射；爆炸质变切换弹体、质变不再出现、两级裂爆=1.5625、一级灼爆=1.2；基础法球/法杖共享 Resource 未变；结算后爆炸特效与活动弹体均清零。
相关回归命令、退出码：tests/smoke 下 42 项快速冒烟（排除并另行执行 soak）全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0，30/60/90/120 秒均 30 个敌人。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察爆炸范围、占位圆环透明与去重手感；E02/D02 正式素材尚未生成），按验证协议标记未完成，不冒充通过。
发现的问题与修复、复测结果：无。实现按“先测试后实现”一次通过，未发现产品逻辑问题。
共享 Resource / 重开 / 空引用检查：爆炸档案、基础法球、法杖、升级 .tres 均未被回写；爆炸倍率与弹体覆盖只存运行时；重开由 ending_restart 回归覆盖；查询对空世界/空 Actor 安全，已释放目标被过滤。
已知问题与下一任务进入条件：E02 爆炸特效、D02 爆炸球为占位，待按素材清单生成；爆炸溅射暂不吸血/不暴击（首版）。T17（火焰持续伤害与火坑）依赖 T16，可以进入；T20 附魔箭将复用本任务的爆炸能力。
```

---

## T17 火焰持续伤害与地面火坑

```text
任务 ID：T17
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T16 已提交；docs/后续新加玩法（空文件）保持未跟踪，未触碰。
本次规则决策（链接规则决策 ID）：新增 D14（已确认·首版）——DoT 每 0.5s 一跳、持续 2.0s；同 ID 刷新不叠层；带 dot 标签不反伤、不暴击、不吸血；火舌命中挂 DoT 与地面火坑分开实现；火坑按 tick 对范围内敌人结算、多个火坑独立叠算；数值全部在 Resource。
修改文件与作用：
- data/combat/damage_over_time_effect.gd、flame_dot.tres（新增）：DoT 可配档案（tick/时长/每跳伤害/预留暴击吸血）。
- scripts/components/status_effect_component.gd（新增）：StatusEffectComponent 管理 Actor 身上的 DoT，按 ID 刷新不叠层，提供 apply_dot/advance_time/has_dot/get_dot_remaining/clear 与施加/结算信号。
- data/combat/ground_damage_area_definition.gd、fire_patch.tres（新增）：地面区域可配档案（半径/时长/tick/伤害/掩码/上限/表现）。
- scripts/combat/ground_damage_area.gd（新增）：GroundDamageArea 区域持续伤害，独立于火舌 DoT；复用 AreaHitResolver 去重查询，来源离树后继续结算。
- data/projectiles/staff_flame_bolt.tres（新增）：火焰弹（命中挂 flame_dot 并生成 fire_patch）。
- data/projectiles/projectile_definition.gd（修改）：新增 damage_over_time 与 ground_area 字段。
- scripts/combat/projectile_base.gd（修改）：命中后调用 _apply_on_hit_effects() 挂 DoT/生成火坑，与爆炸分离。
- scripts/actors/actor_base.gd（修改）：_ready 自动创建 StatusEffectComponent，initialize/die 清空状态；新增 apply_damage_over_time()。
- data/upgrades/upgrade_definition.gd（修改）：新增 FLAME、AREA_DURATION 类型。
- scripts/actors/player_actor.gd（修改）：FLAME 走弹体覆盖，AREA_DURATION 写运行时持续时间倍率。
- data/upgrades/staff_flame.tres、staff_flame_dot_up.tres、staff_flame_duration_up.tres（新增）：火焰质变与烈燃/余烬专属升级。
- scripts/combat/weapon_runtime_modifier.gd、projectile_spawn_context.gd、scripts/components/weapon_controller.gd（修改）：新增 ground_area_duration_multiplier 运行时字段与 getter。
- scripts/core/game_session.gd（修改）：结算 _stop_combat_nodes() 同时清理 GroundDamageArea。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 3 项火焰升级。
- tests/smoke/flame_dot_smoke_test.gd（新增）：7 组断言，使用 advance_time 固定步长保证 tick 确定。
- tests/smoke/bow_multishot_smoke_test.gd（修改·仅测试）：修复 T16 之前就存在的首帧自动发射导致的弹体计数抖动；确认与本任务玩法无关。
新增/修改公共接口及兼容方式：
- DamageOverTimeEffect（Resource）；StatusEffectComponent（Node）、apply_dot/advance_time/has_dot/get_active_dot_count/get_dot_remaining/clear、dot_applied/dot_ticked 信号。
- GroundDamageAreaDefinition（Resource）；GroundDamageArea（Node2D）、setup/advance_time/finish、tick_damage 信号。
- ActorBase.status_effect_component、apply_damage_over_time()。
- ProjectileDefinition.damage_over_time/ground_area；ProjectileSpawnContext.ground_area_duration_multiplier；WeaponRuntimeModifier.ground_area_duration_multiplier；WeaponController.get_ground_area_duration_multiplier()。
- UpgradeType.FLAME/AREA_DURATION。
- 兼容：新字段默认空/null，旧弹体、旧武器与新测试行为不变；StatusEffectComponent 由 ActorBase 自动创建，不改动任何场景。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t17_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t17_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/flame_dot_smoke_test.gd` → 0。断言：2.0s/0.5s 精确 4 跳共 12 点且结束移除；同 ID 刷新仍为 1 层、每跳仍为 3、剩余时间被重置；DoT 不吸血且不触发目标反伤、照常对被反伤目标造成 8 点；来源离树后继续结算、敌人死亡清除状态；火焰质变切换火焰弹、一级余烬=1.25、命中挂 flame_dot 并生成 1 个火坑；火坑只伤范围内、离开后不再受伤、两个重叠火坑各自结算；结算后火坑清零。
相关回归命令、退出码：tests/smoke 下 43 项快速冒烟全部退出码 0（含修复后的 bow_multishot 连续 8 次稳定通过）；120 秒 enemy_spawner_soak_test.gd → 0，30/60/90/120 秒均 30 个敌人。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察火舌/火坑范围、占位地面圆环透明与 tick 手感；E02/D02 正式素材尚未生成），标记未完成。
发现的问题与修复、复测结果：实现中出现两处：1）刷新 DoT 时未重置 tick 计数导致刷新后不再跳伤，已在 apply_dot 重置 elapsed/ticks_done；2）来源被释放后仍把已释放对象传入 DamageEvent，已在推进与结算前用 is_instance_valid 清理并改用安全引用，复测无错误。
共享 Resource / 重开 / 空引用检查：DoT/火坑档案、火焰弹、升级 .tres 未被回写；状态与区域只存运行时；空 source/已释放 source 安全；重开由 ending_restart 回归覆盖；结算清理火坑。
已知问题与下一任务进入条件：E02 火焰特效与 D02 火焰弹为占位，待按素材清单生成；火坑为一次性地面区域，未做贴图分层；DoT 首版不叠层不吸血不暴击。T18（威能分支）依赖 T15/T05，可以进入；T20 附魔箭将复用本任务的 DoT 能力。
```

---

## T18 威能分支

```text
任务 ID：T18
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T17 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D15（已确认·首版）——“射速”和“攻击间隔”是同一冷却参数，仅一个运行时冷却倍率，避免重复乘算；威能质变用可导出 WeaponRuntimeModifier 捆绑伤害/攻速/射程；专属升级独立上限；单武器射程倍率与角色级全武器射程相乘；冷却有全局下限。
修改文件与作用：
- scripts/combat/weapon_runtime_modifier.gd（修改）：由 RefCounted 改为可导出的 Resource，字段加 @export，新增 range_multiplier；使升级资源可在 Inspector 引用捆绑修正。
- data/combat/staff_might_modifier.tres（新增）：威能捆绑修正（冷却 0.8、伤害 1.4、射程 1.25）。
- data/upgrades/upgrade_definition.gd（修改）：新增 WEAPON_RANGE、WEAPON_MODIFIER 类型与 weapon_modifier 字段。
- scripts/actors/player_actor.gd（修改）：apply_upgrade 先应用 weapon_modifier 捆绑修正，再按 type 处理；新增 WEAPON_RANGE（单武器射程倍率）与 WEAPON_MODIFIER（无额外类型效果）。
- scripts/components/weapon_controller.gd（修改）：新增 _range_multiplier 与 get_range_multiplier()，在 get_effective_target_range 中与角色级倍率相乘并重置。
- data/upgrades/staff_might.tres、staff_might_power_up.tres、staff_might_haste_up.tres、staff_might_range_up.tres（新增）：威能质变与崩解/疾涌/远震专属升级。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 4 项威能升级。
- tests/smoke/staff_might_smoke_test.gd（新增）：3 组断言。
新增/修改公共接口及兼容方式：
- WeaponRuntimeModifier 现为 Resource（class_name 不变），字段可导出；既有代码构造方式不变。
- UpgradeDefinition.weapon_modifier；UpgradeType.WEAPON_RANGE/WEAPON_MODIFIER。
- WeaponController.get_range_multiplier()。
- 兼容：range_multiplier 默认 1.0，未使用威能分支的武器索敌值不变；D06 角色级全武器射程语义保留，两者相乘。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t18_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t18_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/staff_might_smoke_test.gd` → 0。断言：质变后冷却 1.6、伤害 ×1.4、有效射程 1000×1.25=1250；共享法杖冷却未被回写；通用 +20% 与威能相乘为 1.68，晚获取长弓只继承通用 1.2；三级崩解=1.4×1.15³、三级疾涌=2.0×0.8×0.9³、三级远震=1000×1.25×1.1³；连续施加极小冷却倍率钳制到 0.02；已选威能后爆炸/火焰分支不可再出。
相关回归命令、退出码：tests/smoke 下 44 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际感受威能法杖的射速/射程与卡面数值；D02 威能球、C04 图标尚未生成），标记未完成。
发现的问题与修复、复测结果：无。WeaponRuntimeModifier 改为 Resource 后既有构造与 .tres 加载均正常。
共享 Resource / 重开 / 空引用检查：共享法杖、弹体、升级 .tres 未被回写；捆绑修正只写运行时；重开由 ending_restart 回归覆盖。
已知问题与下一任务进入条件：D02 威能球、C04 图标为占位；威能质变数值为首版。T19（寒冰减速/冻结/分裂）依赖 T15/T05，可以进入。
```

---

## T19 寒冰减速/冻结/分裂

```text
任务 ID：T19
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T18 已提交；docs/后续新加玩法（空文件）保持未跟踪。任务卡允许按“减速→冻结→分裂”拆三次测试/提交，本次按三个专项测试实现，合并在一个提交中。
本次规则决策（链接规则决策 ID）：新增 D16（已确认·首版）——减速/冻结同 ID 刷新不叠层，移速取最强减速、冻结为 0；Boss 控制免疫；冰枪穿透 + 命中左右对称分裂，小冰枪不再分裂；数值全部 Resource。
修改文件与作用：
- scripts/components/status_effect_component.gd（修改）：新增 SlowState/FreezeState 与 apply_slow/apply_freeze/get_move_speed_multiplier/is_frozen/get_*_remaining；推进与清理纳入统一 advance_time/clear。
- data/combat/movement_slow_effect.gd、ice_slow.tres、data/combat/freeze_effect.gd、ice_freeze.tres（新增）：减速与冻结可配档案。
- scripts/actors/actor_base.gd（修改）：apply_movement_slow/apply_freeze/get_status_move_speed_multiplier/is_control_immune。
- scripts/actors/enemy_actor.gd（修改）：追踪速度乘以状态移速倍率；is_control_immune 读取 EnemyDefinition.control_immune。
- data/enemies/enemy_definition.gd、boss_default.tres（修改）：新增 control_immune，Boss 设为免疫。
- data/projectiles/projectile_definition.gd（修改）：新增 on_hit_slow/on_hit_freeze 与 split_projectile/split_count/split_spread_degrees/split_damage_multiplier/split_speed_multiplier。
- scripts/combat/projectile_base.gd（修改）：命中施加减速/冻结并按对称角度分裂小弹体，is_split_child 防递归。
- scripts/combat/projectile_spawn_context.gd、weapon_runtime_modifier.gd、components/weapon_controller.gd（修改）：新增 freeze_duration_multiplier 与 split_count_bonus 运行时字段与 getter。
- data/projectiles/staff_ice_spear.tres、staff_ice_split.tres（新增）：冰枪（穿透 3/冰缓/冻结/分裂 2）与小冰枪（穿透 1/冰缓/不分裂）。
- data/upgrades/staff_ice.tres、staff_ice_pierce_up.tres、staff_ice_freeze_up.tres、staff_ice_split_up.tres（新增）：寒冰质变与穿刺/冰封/裂冰专属升级。
- data/upgrades/upgrade_definition.gd、scripts/actors/player_actor.gd（修改）：新增 ICE/FREEZE_DURATION/SPLIT_COUNT 类型并应用。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 4 项寒冰升级。
- tests/smoke/ice_slow_smoke_test.gd、ice_freeze_smoke_test.gd、ice_split_smoke_test.gd（新增）：分别覆盖三阶段。
新增/修改公共接口及兼容方式：
- MovementSlowEffect、FreezeEffect（Resource）；StatusEffectComponent 减速/冻结接口与信号。
- ActorBase.apply_movement_slow/apply_freeze/get_status_move_speed_multiplier/is_control_immune/is_frozen。
- ProjectileDefinition.on_hit_slow/on_hit_freeze/split_*；ProjectileSpawnContext.freeze_duration_multiplier/split_count_bonus/is_split_child；WeaponRuntimeModifier.freeze_duration_multiplier/split_count_bonus；WeaponController.get_freeze_duration_multiplier()/get_split_count_bonus()。
- UpgradeType.ICE/FREEZE_DURATION/SPLIT_COUNT。
- 兼容：新字段默认空/null/1.0，旧弹体与敌人行为不变；EnemyDefinition.control_immune 默认 false。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t19_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t19_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：
- `--script res://tests/smoke/ice_slow_smoke_test.gd` → 0：分支切换冰枪；穿透命中两个敌人各 7 点且剩余穿透 1；命中施加冰缓；冻结结束后移速 0.6、减速结束后 1.0；Boss 拒绝减速；敌人死亡清空状态；共享冰缓比例/穿透未变。
- `--script res://tests/smoke/ice_freeze_smoke_test.gd` → 0：冻结期间移速 0；0.8s 后回到冰缓 0.6，1.5s 后恢复 1.0；一级冰封使冻结剩余 1.0s；重复冻结刷新不叠层；Boss 拒绝冻结；共享冻结时长未变。
- `--script res://tests/smoke/ice_split_smoke_test.gd` → 0：命中分裂 2 枚小冰枪且不影响主冰枪穿透；小冰枪命中不再分裂；两级裂冰使分裂数为 4 且达上限不再出现；分裂弹落在 ±45°、方向不同、伤害倍率 0.4；共享资源未变。
相关回归命令、退出码：tests/smoke 下 47 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察冰枪穿透、冻结停步、分裂小冰枪与减速标记；C04/D01/E03 正式素材尚未生成），标记未完成。
发现的问题与修复、复测结果：T19 实现后，T19-a 的既有断言“冻结结束后移速 0.6”因冰枪同时附带冻结而暂时改为先推进 0.8s 再断言，三阶段合测通过；产品逻辑无问题。
共享 Resource / 重开 / 空引用检查：减速/冻结/分裂数值只在 Resource 与运行时；未回写任何 .tres；死亡/离树/重开清理状态；split 对空 shooter 安全跳过。
已知问题与下一任务进入条件：C04 冰枪、D01 小冰枪、E03 冰霜特效为占位；分裂为一次命中生成、非持续。T20（附魔箭与法杖分支联动）依赖 T12 与 T16～T19，现均已完成，可以进入。
```

---

## T20 附魔箭与法杖分支联动

```text
任务 ID：T20
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T19 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：D08（快照继承，已确认）与新增 D17（已确认·首版）：四张独立弓质变卡共享 branch_id=enchant 实现互斥；候选需持弓、弓基础满级并已持有对应分支的法杖；四分支效果映射复用既有能力且数值独立。
修改文件与作用：
- data/upgrades/upgrade_definition.gd（修改）：新增 ENCHANT_ARROW 类型与 required_equipment_id/required_equipment_branch_id 前置装备字段。
- scripts/systems/upgrade_system.gd（修改）：can_offer 增加前置装备与分支过滤。
- scripts/actors/player_actor.gd（修改）：ENCHANT_ARROW 切换弓的弹体；威能附魔复用 weapon_modifier 捆绑修正。
- data/projectiles/arrow_explosion.tres、arrow_flame.tres、arrow_ice.tres、arrow_power.tres（新增）：四种附魔箭（复用现有爆炸/DoT+火坑/减速+冻结/威能箭）。
- data/combat/arrow_power_modifier.tres（新增）：威能箭独立捆绑修正（伤害 1.3 / 冷却 0.85 / 射程 1.2）。
- data/upgrades/enchant_explosion.tres、enchant_flame.tres、enchant_ice.tres、enchant_power.tres（新增）：四张附魔箭质变卡。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 4 张附魔卡。
- tests/smoke/enchant_arrow_smoke_test.gd（新增）：候选资格、四映射、互斥与共享 Resource。
- tests/smoke/staff_charge_smoke_test.gd（修改·仅测试）：修复与 bow_multishot 同源的“首帧起始武器自动发射”弹体计数抖动。
新增/修改公共接口及兼容方式：
- UpgradeDefinition.required_equipment_id/required_equipment_branch_id；UpgradeType.ENCHANT_ARROW。
- 兼容：前置字段默认为空，旧升级不受影响；附魔只影响弓的运行时弹体覆盖，不写共享 Resource。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t20_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t20_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/enchant_arrow_smoke_test.gd` → 0。断言：无弓/无杖/杖未质变均不可出；爆炸法杖只解锁爆炸附魔；爆炸附魔切换为 arrow_explosion 并带爆炸档案，且与多重/万箭及其它附魔互斥；火焰附魔带 DoT 与火坑；寒冰附魔带减速与冻结；威能附魔切换 arrow_power 且长弓伤害 ×1.3、冷却 ×0.85、射程 ×1.2；共享普通箭与长弓弹体未被回写。
相关回归命令、退出码：tests/smoke 下 48 项快速冒烟全部退出码 0（staff_charge 修复后连续 8 次稳定通过）；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察四种附魔箭外观与效果、候选卡文案；D02 附魔箭素材尚未生成），标记未完成。
发现的问题与修复、复测结果：回归中发现 `staff_charge_smoke_test` 与 bow_multishot 同源的首帧起始武器弹体计数抖动，已在该测试中移除首帧残留弹体，复测与全量回归稳定。
共享 Resource / 重开 / 空引用检查：四张附魔卡、四种箭、威能修正 .tres 未被回写；附魔只写弓控制器运行时覆盖；重开由 ending_restart 回归覆盖。
已知问题与下一任务进入条件：D02 附魔箭为占位、无专属图标；附魔箭无独立质变专属升级（首版）。T21（反伤刺甲）依赖 T11，可以进入。
```

---

## T21 反伤刺甲

```text
任务 ID：T21
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T20 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D18（已确认·首版）——刺圈按周期做一次受控范围结算，受击返还按实际扣血比例且零/闪避/免疫不返还，两路都带 reflect 标签防递归；数值全部 Resource。
修改文件与作用：
- data/armor/thorn_armor_definition.gd、data/armor/armor_thorns.tres（新增）：反伤刺甲档案（刺圈半径/周期/每跳/掩码/返还比例/占位颜色）。
- scripts/components/thorn_aura_component.gd（新增）：ThornAuraComponent 周期刺圈，复用 AreaHitResolver，带 reflect 标签与 advance_time（测试确定）。
- data/upgrades/upgrade_definition.gd（修改）：新增 THORN_AURA/THORN_RADIUS/REFLECT_RATIO 与 thorn_armor 字段。
- scripts/actors/player_actor.gd（修改）：初始化清空刺圈与返还；应用刺甲时挂载刺圈并增加返还比例；专属升级调整半径倍率与返还。
- data/upgrades/armor_thorns.tres、armor_thorns_radius_up.tres、armor_thorns_reflect_up.tres（新增）：反伤刺甲质变与扩刺/锐刺专属升级。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 3 项刺甲升级。
- tests/smoke/thorn_armor_smoke_test.gd（新增）：6 组断言。
新增/修改公共接口及兼容方式：
- ThornArmorDefinition（Resource）；ThornAuraComponent.initialize/set_radius_multiplier/get_effective_radius/get_tick_count/advance_time 与 aura_ticked 信号。
- PlayerActor.get_thorn_aura()；UpgradeType.THORN_AURA/THORN_RADIUS/REFLECT_RATIO；UpgradeDefinition.thorn_armor。
- 兼容：ActorBase 反伤管线未改，刺甲只是设置运行时返还比例；未选刺甲的玩家行为不变。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t21_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t21_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/thorn_armor_smoke_test.gd` → 0。断言：0.5s 刺圈对范围内敌人扣 4、范围外不变、tick 数为 1，1s 累计 8；受击返还 = 实际扣血 ×0.3；零伤害与闪避都不返还；敌人 100% 反伤时刺圈不反噬玩家；三级扩刺刺圈=90×1.2³、三级锐刺返还=0.6、达上限不再出现、已选刺甲不再出现；重新初始化清空刺圈与返还。
相关回归命令、退出码：tests/smoke 下 49 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察刺圈占位圆与返还反馈；C06/E04 正式素材尚未生成），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：刺甲档案与升级 .tres 未被回写；半径/返还只存运行时；重新初始化与重开清空；刺圈对空 Actor/离树安全。
已知问题与下一任务进入条件：C06 图标与 E04 特效待生成；刺圈为占位圆。T22（骑士盔甲）依赖 T11，可以进入。
```

---

## T22 骑士盔甲

```text
任务 ID：T22
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T21 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D19（已确认·首版）——固定减伤=额外固定防御进统一管线；完全免伤=免疫概率，严格受 CombatRules 上限钳制；与反伤刺甲互斥。
修改文件与作用：
- data/armor/knight_armor_definition.gd、data/armor/armor_knight.tres（新增）：骑士盔甲档案（固定减伤、免疫概率）。
- scripts/actors/actor_base.gd（修改）：新增 set_immune_chance() 供初始化/档案写入。
- data/upgrades/upgrade_definition.gd（修改）：新增 KNIGHT_ARMOR/KNIGHT_DEFENSE/KNIGHT_IMMUNE 与 knight_armor 字段。
- scripts/actors/player_actor.gd（修改）：新增 _knight_defense_bonus 并纳入 _refresh_defense；初始化清空免疫；应用骑士档案与坚壁/圣佑专属升级。
- data/upgrades/armor_knight.tres、armor_knight_defense_up.tres、armor_knight_immune_up.tres（新增）：骑士质变与专属升级。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 3 项骑士升级。
- tests/smoke/knight_armor_smoke_test.gd（新增）：5 组断言。
新增/修改公共接口及兼容方式：
- KnightArmorDefinition（Resource）；ActorBase.set_immune_chance()；UpgradeType.KNIGHT_ARMOR/KNIGHT_DEFENSE/KNIGHT_IMMUNE；UpgradeDefinition.knight_armor。
- 兼容：基础盔甲与通用防御升级行为不变；骑士固定减伤只是第三个防御来源并汇总进 _refresh_defense。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t22_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t22_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/knight_armor_smoke_test.gd` → 0。断言：骑士后防御 11+6=17，30 点攻击扣 13；三级坚壁防御 26、30 点扣 4；未选骑士免疫为 0 且正常扣血；默认规则免伤钳制 0.5、上限放开可到 100% 且触发一次免疫信号；100% 免伤不扣血且不触发反伤；骑士与反伤刺甲互斥。
相关回归命令、退出码：tests/smoke 下 50 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察完全免伤提示与减伤反馈；C06/E04 正式素材尚未生成），标记未完成。
发现的问题与修复、复测结果：测试初稿直接用 add_equipment_base_level 升级基础盔甲却未刷新盔甲防御，导致防御断言失败；改用 armor_basic_up 升级卡（会触发 _refresh_armor_bonuses）后通过。产品逻辑无问题。
共享 Resource / 重开 / 空引用检查：骑士档案与升级 .tres 未被回写；防御/免疫只存运行时；初始化清空免疫与骑士减伤。
已知问题与下一任务进入条件：C06/E04 素材待生成；骑士免伤仅概率、上限由 CombatRules 控制。T23（狂战盔甲）依赖 T11，可以进入。
```

---

## T23 狂战盔甲

```text
任务 ID：T23
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T22 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D20（已确认·首版）——失血绕过防御且默认不致死；吸血基础值+半血加成由玩家动态提供、武器生成时读取；一次免死进统一管线、每局一次、DoT 也适用；三档专属升级。
修改文件与作用：
- data/armor/berserk_armor_definition.gd、data/armor/armor_berserk.tres（新增）：狂战档案（失血周期/数值/非致死、基础吸血、半血加成与阈值）。
- scripts/components/berserk_drain_component.gd（新增）：BerserkDrainComponent 周期失血（绕过防御），advance_time 供测试。
- scripts/combat/damage_result.gd（修改）：新增 death_immunity_triggered。
- scripts/actors/actor_base.gd（修改）：一次免死标记与致命拦截；新增 set_death_immunity_available/has_death_immunity/get_bonus_lifesteal_ratio；初始化重置标记。
- scripts/components/weapon_controller.gd（修改）：弹体吸血 = 运行时修正 + 角色额外吸血（狂战），钳制 1.0。
- data/upgrades/upgrade_definition.gd（修改）：新增 BERSERK_ARMOR/BERSERK_DRAIN/BERSERK_LIFESTEAL/BERSERK_IMMUNITY 与 berserk_armor 字段。
- scripts/actors/player_actor.gd（修改）：启用狂战、失血减免、半血增吸血、一次免死；初始化清空。
- data/upgrades/armor_berserk.tres、armor_berserk_drain_up.tres、armor_berserk_lifesteal_up.tres、armor_berserk_immunity.tres（新增）：狂战质变与三档专属升级。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 4 项狂战升级。
- tests/smoke/berserk_armor_smoke_test.gd（新增）：6 组断言。
新增/修改公共接口及兼容方式：
- BerserkArmorDefinition（Resource）；BerserkDrainComponent.initialize/set_drain_reduction/get_effective_drain/get_tick_count/advance_time 与 drained 信号。
- ActorBase.set_death_immunity_available/has_death_immunity/get_bonus_lifesteal_ratio；DamageResult.death_immunity_triggered；PlayerActor.get_berserk_drain/get_bonus_lifesteal_ratio。
- UpgradeType.BERSERK_*；UpgradeDefinition.berserk_armor。
- 兼容：额外吸血默认 0，旧武器结算不变；一次免死默认关闭；失血组件只有启用狂战才存在。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t23_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t23_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/berserk_armor_smoke_test.gd` → 0。断言：每秒失血 3、两秒 6、压到 1 点后不再扣；满血额外吸血 0.15、半血 0.4、三级嗜血 0.7，武器弹体继承 0.7；首次致命触发免死剩 1 点且标记消耗，第二次致命（DoT）死亡；狂战与反伤/骑士互斥；三级忍痛失血为 0；重新初始化清空失血/吸血/免死。
相关回归命令、退出码：tests/smoke 下 51 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察失血节奏、半血吸血与免死反馈；C06/E04 正式素材尚未生成），标记未完成。
发现的问题与修复、复测结果：实现初版半血加成误用运行时累加值而非档案基础值，导致半血吸血为 0；已改为“档案基础 + 运行时升级”后复测通过。
共享 Resource / 重开 / 空引用检查：狂战档案与升级 .tres 未被回写；失血/吸血/免死只存运行时；初始化与重开清空。
已知问题与下一任务进入条件：C06/E04 素材待生成；失血为固定值、无低血量视觉。T24（头盔、手套与科技分支）依赖 T08/T09/T10，可以进入。
```

---

## T24 头盔、手套与科技分支

```text
任务 ID：T24
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T23 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D21（已确认·首版）——头盔小防御无移速惩罚；手套防御+冷却缩短+射程；科技单件每秒经验与宝石倍率，玩家汇总、统一入口、暂停不计时；单件即生效，套装留 T25。
修改文件与作用：
- data/armor/armor_definition.gd（修改）：新增 cooldown_reduction/_per_level、attack_range_bonus/_per_level 与 get_cooldown_multiplier_for_level/get_range_multiplier_for_level。
- data/armor/armor_helmet.tres、armor_gloves.tres（新增）：基础头盔（防御 2/级 +1、无惩罚）与基础手套（防御 1/级 +0.5、冷却 -10%/级 +2%、射程 +10%/级 +2%）。
- data/armor/tech_armor_definition.gd、tech_armor_basic.tres、tech_helmet.tres、tech_gloves.tres（新增）：科技单件档案（每秒经验、宝石倍率）。
- data/upgrades/acquire_helmet.tres、helmet_base_up.tres、acquire_gloves.tres、gloves_base_up.tres、armor_tech.tres、helmet_tech.tres、gloves_tech.tres（新增）。
- data/catalog/default_catalog.tres（修改）：防具目录加入头盔与手套。
- data/upgrades/upgrade_definition.gd（修改）：新增 TECH_ASCENSION 与 tech_armor 字段。
- scripts/actors/actor_base.gd（修改）：新增 get_bonus_cooldown_multiplier/get_bonus_range_multiplier 默认 1.0。
- scripts/components/weapon_controller.gd（修改）：有效冷却与索敌射程纳入角色手套倍率。
- scripts/actors/player_actor.gd（修改）：_refresh_armor_bonuses 汇总手套冷却/射程；科技单件记录、每秒经验、宝石倍率、advance_tech_time 与 _process；初始化清空。
- scripts/pickups/experience_gem.gd（修改）：宝石经验乘玩家倍率，单颗只结算一次。
- scenes/gameplay/game_session.tscn（修改）：升级池加入 7 项。
- tests/smoke/helmet_gloves_tech_smoke_test.gd（新增）：5 组断言。
新增/修改公共接口及兼容方式：
- ArmorDefinition.get_cooldown_multiplier_for_level/get_range_multiplier_for_level。
- ActorBase.get_bonus_cooldown_multiplier/get_bonus_range_multiplier（默认 1.0，旧行为不变）。
- TechArmorDefinition（Resource）；UpgradeType.TECH_ASCENSION；UpgradeDefinition.tech_armor。
- PlayerActor.get_tech_experience_per_second/get_experience_gain_multiplier/advance_tech_time/get_bonus_cooldown_multiplier/get_bonus_range_multiplier。
- 兼容：手套/头盔未持有时倍率 1.0、防御汇总不变；宝石默认倍率 1.0，旧经验测试不变。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t24_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t24_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/helmet_gloves_tech_smoke_test.gd` → 0。断言：头盔 2 + 手套 1 防御=3，手套后冷却 0.9、射程 990；满级防御 9、冷却 0.82、射程 1.18；单件科技每秒经验 2、宝石倍率 1.15、1 秒 +2 经验；三件科技每秒 7、宝石倍率 1.25×1.15×1.15、2 秒 +14，宝石 10 点按倍率结算且不可重复拾取；未满级不能选科技、科技战甲与反伤刺甲互斥；重新初始化清空手套与科技。
相关回归命令、退出码：tests/smoke 下 52 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察头盔/手套图标、攻速射程变化与科技每秒经验；C07/D05 图标尚未生成），标记未完成。
发现的问题与修复、复测结果：全量回归中 T23 狂战吸血弹体断言因“首帧起始武器已进入冷却”偶发失败，已在测试内先 reset_runtime_state 再发射；连续 6 次稳定。
共享 Resource / 重开 / 空引用检查：头盔/手套/科技档案与升级 .tres 未被回写；倍率与科技状态只存运行时；初始化与重开清空。
已知问题与下一任务进入条件：C07/D05 图标待生成；科技单件无专属升级（首版）。T25（科技三件套与发射器）依赖 T24/D09，可以进入；需先定导弹与激光是否同时获得（D09 留待实现时按可配置分支处理）。
```

---

## T25 科技三件套与发射器

```text
任务 ID：T25
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T24 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D22（已确认·首版，用户确认）：三件都需科技质变（D09）；状态只在切换时应用/撤销一次；高移速为可撤销运行时乘数、飞行仅视觉不穿墙；发射器为独立数据驱动武器，随机目标取自 TargetingService，无候选回退基础方向；激光留作后续可配置分支不实现。
修改文件与作用：
- data/armor/tech_set_definition.gd、tech_set_default.tres（新增）：套装配置（所需三件 ID/分支、移速倍率、飞行视觉偏移、发射器武器）。
- data/combat/missile_explosion_profile.tres、data/projectiles/tech_missile.tres、data/weapons/tech_launcher.tres（新增）：导弹爆炸档案、导弹、发射器（3 发、随机索敌 target_mode=1）。
- data/weapons/weapon_definition.gd（修改）：新增 TargetMode 枚举与 target_mode 字段。
- scripts/systems/targeting_service.gd（修改）：新增 get_random_target 与 set_random_seed（只遍历缓存）。
- scripts/components/weapon_controller.gd（修改）：_fire_volley 支持逐发随机目标，取不到回退基础方向。
- scripts/actors/player_actor.gd（修改）：套装判定/激活/撤销、可撤销移速、飞行视觉偏移、授予/移除发射器（不占六格）、tech_set_activated/deactivated 信号；clear_weapons 清空套装武器；初始化重置。
- scripts/core/game_session.gd（修改）：新增 tech_set_definition 导出并在 start_run 注入玩家。
- scenes/gameplay/game_session.tscn（修改）：挂载默认科技套装配置。
- tests/smoke/tech_set_smoke_test.gd（新增）：5 组断言。
新增/修改公共接口及兼容方式：
- TechSetDefinition（Resource）；WeaponDefinition.TargetMode/target_mode。
- TargetingService.get_random_target/set_random_seed。
- PlayerActor.configure_tech_set/is_tech_set_active/get_set_weapon/grant_set_weapon/remove_set_weapon、tech_set_activated/deactivated。
- GameSession.tech_set_definition。
- 兼容：默认 NEAREST 索敌与旧武器不变；无套装配置时玩家行为不变；发射器不占六格。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t25_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t25_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/tech_set_smoke_test.gd` → 0。断言：仅战甲/两件时不激活，三件科技激活后移速 308 且只有一个发射器；重复 configure 不重复加速/不新增发射器；盔甲选反伤时套装不激活、无发射器；一次发射生成 3 枚带爆炸导弹且方向不全相同；范围内无候选时回退仍发射 3 枚；clear_weapons 后套装武器引用为空且无发射器控制器。
相关回归命令、退出码：tests/smoke 下 53 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察飞行视觉、套装激活反馈与导弹爆炸手感；C08/E05/D01 正式素材尚未生成），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：套装/导弹/发射器 .tres 未被回写；移速/发射器只存运行时；clear_weapons 与重开清理；随机目标对空候选安全。
已知问题与下一任务进入条件：C08/E05/D01 素材待生成；激光未实现（后续可配置分支）；导弹为直线弹（无制导）。T26（金币掉落与一次性结算）依赖 T01，可以进入。
```

---

## T26 金币掉落与一次性结算

```text
任务 ID：T26
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T25 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D23（已确认·首版，沿用 D10 数值）：掉率 15%、每次 1 金、结算每击杀 2 金，全部在 EconomyRules；拾取原子一次；结算区分拾取与击退且只发一次；持久化留 T27。
修改文件与作用：
- data/economy/economy_rules.gd、default_economy_rules.tres（新增）：可配掉率/每次数量/每击杀奖励与 roll_drop。
- scripts/pickups/coin_pickup.gd、scenes/pickups/coin_pickup.tscn（新增）：金币拾取物（同层原子结算，复用宝石帧并调成金色占位）。
- scripts/core/game_result.gd（修改）：新增 coins_collected/coins_from_kills/total_coins（构造参数默认 0，兼容旧调用）。
- scripts/actors/player_actor.gd（修改）：coins_changed 信号、_run_coins、add_coins/get_run_coins，初始化归零，拾取路由支持 CoinPickup。
- scripts/core/game_session.gd（修改）：economy_rules/coin_pickup_scene 导出、私有随机源与 set_economy_random_seed、spawn_coin_pickup/_spawn_coin_drop、结算写入金币明细。
- scripts/ui/end_panel.gd、session_controls.gd（修改）：结算面板与局内装备栏显示金币。
- scenes/gameplay/game_session.tscn（修改）：挂载经济规则与金币场景。
- tests/smoke/coin_economy_smoke_test.gd（新增）：4 组断言。
新增/修改公共接口及兼容方式：
- EconomyRules（Resource，roll_drop）；CoinPickup（class_name）initialize/collect/get_amount/is_collected 与 collected 信号。
- PlayerActor.add_coins/get_run_coins、coins_changed；GameSession.economy_rules/coin_pickup_scene/spawn_coin_pickup/set_economy_random_seed/get_run_coins。
- GameResult 新增金币字段；旧四参构造与既有测试不变。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t26_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t26_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/coin_economy_smoke_test.gd` → 0。断言：金币首次拾取成功、重复失败、余额 3、信号一次；必定掉金时击杀生成金币、重复死亡不重复计数；结算结果拾取 2 + 击退 5 = 7、重复结算不再发奖；重开后余额 0、地面金币清空；零击杀总奖励 0。
相关回归命令、退出码：tests/smoke 下 54 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需在 1280×720 实际观察金币掉落/拾取动画与结算面板金币明细；D03 正式金币素材尚未生成），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：经济规则 .tres 未被回写；金币余额只存运行时；重开由场景重建清空；掉率随机源可固定种子。
已知问题与下一任务进入条件：D03 金币素材待生成；金币仅内存，未持久化。T27（版本化存档与安全读写）依赖 T26，可以进入。
```

---

## T27 版本化存档与安全读写

```text
任务 ID：T27
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T26 已提交；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策（链接规则决策 ID）：新增 D24（已确认·首版）：Profile 只存标量与稳定 ID；读档校验/迁移/丢未知 ID；原子写档并保留旧档；默认解锁由入口按目录填充；测试用独立临时路径。
修改文件与作用：
- scripts/core/profile.gd（新增）：Profile 数据对象，to_dict/from_dict（校验、v1 迁移、未知 ID 过滤）、copy、add_coins、解锁查询。
- scripts/core/profile_store.gd（新增）：ProfileStore 安全读写，临时文件+校验+备份+替换，last_error、默认/已知 ID、write_failure_injected、delete_save。
- scripts/core/game_entry.gd（修改）：持有 ProfileStore，启动加载档案，start session 后连接 run_ended，apply_run_result 累加金币并写档，失败时给菜单状态；暴露 get_profile/get_profile_store。
- scripts/ui/main_menu.gd（修改）：新增 set_status/get_status，提示栏显示存档状态。
- tests/smoke/profile_store_smoke_test.gd（新增）：9 组断言。
- tests/smoke/experience_flow_smoke_test.gd（修改·仅测试）：关闭金币掉落，避免随机掉金污染经验拾取计数。
新增/修改公共接口及兼容方式：
- Profile（class_name，CURRENT_VERSION=1）；ProfileStore（class_name）。
- GameEntry.profile_store/profile/get_profile/get_profile_store/apply_run_result；MainMenu.set_status/get_status。
- 兼容：main.tscn 直启不经过 GameEntry，玩法不变；档案读写不影响单局数据。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t27_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t27_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/profile_store_smoke_test.gd` → 0。断言：首次无档返回默认解锁+0 金；保存后新实例读回金币/刷新/永久强化/解锁一致；损坏 JSON 回退默认、记录错误且不改原文件；缺字段用默认值、未知 ID 被丢弃；v0 迁移到 v1 且保留金币；注入写入失败返回 false 且旧档金币不变；连续两次结算累加到 12；GameEntry 两局累加到 15 并持久化、写入失败时菜单显示状态。
相关回归命令、退出码：tests/smoke 下 55 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工检查**（需实际重启进程、观察菜单存档状态与结算金币跨局累计），标记未完成。
发现的问题与修复、复测结果：初版测试用例共用一个存档文件导致互相污染、且 JSON.parse_string 在损坏档上打印引擎错误；已改为每用例独立路径、改用 JSON 实例解析。另发现 T26 掉金使既有 experience_flow 拾取计数偶发失败，已在测试内关闭掉金。
共享 Resource / 重开 / 空引用检查：档案只存 user:// 独立路径，未写任何 .tres；测试路径独立于正式 player.json；读写失败保留旧档。
已知问题与下一任务进入条件：正式存档路径与菜单页面在 T29/T30 继续接入；备份策略为单份 .bak。T28（升级选项刷新次数）依赖 T06/T27，可以进入。
```

---

## 素材接入：T16–T26 战斗素材（2026-09-25）

```text
任务 ID：素材接入（跨 T16–T26，非编号任务）
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T27 已提交；外部 AI 按请求新增 84 个透明 PNG 与交付清单（未跟踪）；docs/后续新加玩法（空文件）保持未跟踪。
本次规则决策：沿用 D13–D24 与素材清单；不改玩法数值；新增的唯一玩法相关配置是 WeaponDefinition.visual_scale（把原控制器硬编码的手持缩放迁出）。
修改文件与作用：
- assets/v2_dark_comic/{weapons,projectiles,fx,pickups,ui}/（新增 84 PNG + 交付清单）。
- tools/build_combat_visual_resources.py、tools/wire_combat_visuals.py（新增）：生成 19 个 SpriteFrames 并批量接入/写图标。
- data/visuals/v2_dark_comic/*_frames.tres（新增 19 个）：武器/弹体/特效/金币帧。
- data/projectiles/*.tres（修改 11）：visual_frames 与 visual_scale。
- data/weapons/{bow,staff,tech_launcher}.tres 与 weapon_definition.gd、weapon_controller.gd（修改）：手持贴图与可配置 visual_scale。
- data/combat/{staff_orb_explosion_profile,missile_explosion_profile,fire_patch,ice_freeze}.tres、freeze_effect.gd、thorn_armor_definition.gd、armor_thorns.tres（修改）：特效帧接入；StatusEffectComponent/ThornAuraComponent/GroundDamageArea 显示序列帧，无素材回退占位。
- scenes/pickups/coin_pickup.tscn（修改）：金币四帧。
- data/armor/*.tres、data/upgrades/*.tres（修改 25）：写入 icon。
- scripts/ui/level_up_panel.gd（修改）：升级卡显示图标。
- tests/smoke/new_combat_art_preview.gd、new_asset_sheet_preview.gd（新增）：图形模式预览与总览截图脚本，不参与 headless 回归。
- art_review/v2_new_combat_art_preview.png、v2_asset_sheet_preview.png、v2_ingame_preview.png（更新）：实拍证据。
公共接口：WeaponDefinition.visual_scale（默认 0.22，兼容旧武器）；FreezeEffect.visual_frames/visual_scale；ThornArmorDefinition.visual_frames/visual_scale。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析/启动：`--editor --quit` → 0；84 个 PNG 导入成功；`--quit-after 300` → 0。
回归：tests/smoke 55 项快速冒烟全部 0；120 秒 soak → 0。
GUI / 人工检查：**已执行图形模式实拍**（Metal / Apple M5），截图 `art_review/v2_new_combat_art_preview.png` 与 `v2_asset_sheet_preview.png`。所见：长弓/法杖/科技发射器手持可见；箭与四款附魔箭、法球/爆炸法球/火焰弹/冰枪/小冰枪/导弹均按各自贴图显示；爆炸、火坑、冰冻结界、反伤刺圈、金币四帧正常；升级卡图标显示；无白底/丢图/水印。
发现的问题与修复：首版弹体按 0.16–0.24 缩放偏小，已把弹体调至 0.24–0.38、手持武器统一 0.3；`GroundDamageArea`/冻结/刺圈原先没有序列帧显示，已补上并保留无素材时的占位回退。
已知问题：C08 激光未实现；第四批素材（射手、剑、主菜单版式、敌人/地图）未制作；预览脚本退出时有对象未释放的 warning（仅预览，不影响游戏与回归）。
```

---

## T28 升级选项刷新次数

```text
任务 ID：T28
日期 / 分支 / 提交：2026-09-25 / feat/dark-comic-art / 见本次提交
实施前 Git 状态与已有改动：T01～T27 与素材接入已提交；工作树干净。
本次规则决策（链接规则决策 ID）：新增 D25（已确认·首版）——每局刷新 = 基础 + 档案加成；刷新消耗一次、优先换卡、不自动应用；跨升级保留、重开归零；零次/非选择态/空池禁用。
修改文件与作用：
- scripts/systems/upgrade_system.gd（修改）：新增 _remaining_refreshes、set_refresh_count/get_remaining_refreshes/can_refresh/refresh_choices/set_random_seed，抽取 _draw_choices(exclude, count) 支持刷新换卡。
- scripts/ui/level_up_panel.gd（修改）：卡列表下方新增“刷新（剩余 N）”按钮，按可刷新状态启用/禁用，点击调用 refresh_choices。
- scripts/core/game_session.gd（修改）：新增 base_refresh_count 导出与 set_initial_refresh_count，start_run 注入 UpgradeSystem。
- scripts/core/game_entry.gd（修改）：开局按 基础 + Profile.refresh_bonus 注入刷新次数。
- tests/smoke/upgrade_refresh_smoke_test.gd（新增）：5 组断言。
新增/修改公共接口及兼容方式：
- UpgradeSystem.set_refresh_count/get_remaining_refreshes/can_refresh/refresh_choices/set_random_seed。
- GameSession.base_refresh_count/set_initial_refresh_count；LevelUpPanel 刷新按钮。
- 兼容：默认 base_refresh_count=1；旧直启与旧测试不受影响，原有均匀三选一逻辑不变。
Godot 版本和可执行文件：Godot 4.7.1.stable.official.a13da4feb；/Users/taochenhui/codesource/Godot.app/Contents/MacOS/Godot
解析命令、退出码与 Output：`--headless --log-file /tmp/t28_parse.log --path . --editor --quit` → 0；无解析错误。
启动命令、退出码与 Output：`--headless --log-file /tmp/t28_boot.log --path . --quit-after 300` → 0；无阻断性错误。
专项测试命令、退出码与断言：`--script res://tests/smoke/upgrade_refresh_smoke_test.gd` → 0。断言：0 次不可刷新且 refresh 返回 false；两次刷新依次剩 1/0 且换出不同卡；空池给空列表且不可刷新；跨升级保留剩余次数；直启用基础次数，GameEntry 注入 基础+档案加成=3。
相关回归命令、退出码：tests/smoke 下 56 项快速冒烟全部退出码 0；120 秒 enemy_spawner_soak_test.gd → 0。
GUI / 人工检查步骤、实际结果；未执行的原因：**未执行 GUI 人工点击刷新**（按钮位于升级面板，需真实窗口点击确认暂停态可操作与文案），标记未完成。
发现的问题与修复、复测结果：无。
共享 Resource / 重开 / 空引用检查：刷新次数只存运行时；档案加成只读复制；重开由新场景归零；空池安全。
已知问题与下一任务进入条件：刷新按钮尚未做 GUI 点击验收；永久刷新加成购买属 T30。T29（武器/角色解锁与购买）依赖 T02/T27，可以进入。
```










